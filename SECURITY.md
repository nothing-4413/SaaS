# 安全策略

## 支持版本

仅支持 `main` 分支的最新提交。

## 报告漏洞

请不要在公开 Issue 中披露安全漏洞。请通过仓库维护者可用的私密渠道提交复现步骤、影响范围和建议修复方案。确认后会尽快评估、修复并发布安全公告。

## 内置安全措施

- 所有领域资源按组织隔离。
- 权限中间件拒绝缺少身份或权限不足的请求。
- Bearer Token 使用 HMAC 签名并校验过期时间。
- Webhook 使用 HMAC-SHA256 签名和幂等键。
- 库存和订单操作拒绝负数或非法数量。
